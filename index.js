/**
 *
 * @param {Object} config Configuration JSON, its schema is found in
 * @returns Function that reads the documents defined in config, generates output sub-documents and writes them in the corresponding paths
 */
const subdocument = (config) => () => {};
